import React from "react";

// FinancialHub (temporary replacement). This file is used while the original
// `FinancialHub.tsx` is being cleaned. Replace and expand functionality
// incrementally from this file.
export default function FinancialHub(): JSX.Element {
  return (
    <main className="min-h-screen p-8">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-3xl font-bold mb-6">Financial Hub (temporary)</h1>
        <p className="text-muted-foreground">
          The Financial Hub is temporarily simplified to restore a clean build.
          Functionality will be reintroduced in small, verified steps.
        </p>
      </div>
    </main>
  );
}
